# 인증키가 들어 있다. 절대 커밋하지 않는다.
.env
.env.local
.env.*.local

# 파이썬
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# 사용 기록 DB (운영 데이터)
data/

# 배포 바이너리는 저장소에 넣지 않는다(용량). 서버 디스크에만 둔다.
web/dist/*.exe
web/dist/*.zip

# 설치 프로그램 빌드 부산물. install.py 와 build.py 만 저장소에 둔다.
installer/build/
installer/dist/
installer/*.spec

# 산출물 · 시험 결과
output/
*.dxf
preview*.svg
cad_test.dxf
e2e_download.dxf

# 실행 흔적
server.log
server.err
*.log

# 편집기 · OS
.vscode/
.idea/
Thumbs.db
desktop.ini
